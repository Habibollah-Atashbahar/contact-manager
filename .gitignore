# Docker (نیاز نیست برای Vercel)
Dockerfile
docker-compose.local.yml

# .next build (ignore کن)
frontend/.next/
frontend/out/

# node_modules
frontend/node_modules/

# Environment (already have)
.env
.env.local
.env.production
