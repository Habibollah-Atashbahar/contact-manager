version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=sqlite://database.db
      - JWT_SECRET=your-secret-key-2024
      - APP_ENV=development
    volumes:
      - ./backend:/app